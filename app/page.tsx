import App from '@/components/App'

// This page uses client-side features (localStorage, Supabase),
// so the actual App component is a Client Component
export default function Home() {
  return <App />
}
